# RTE 使用手册

Rollingcats Transit Expansion（modId `rte`）—— MTR 4.0.x 的轨道几何与建设工具扩展，兼带旧版资源包兼容层。

作者：Rolling-Catawa

---

## 运行环境

| 项 | 要求 |
|---|---|
| Minecraft | 1.20.1 |
| 加载器 | Fabric / Forge（Architectury） |
| 前置 | MTR 4.0.x（必需） |
| 可选前置 | MSD（MTR Station Decoration）—— 接触网相关功能需要 |
| 服务端 | 建议同样安装本模组 —— 数据同步功能需要，缺失时自动退回原有行为 |

---

## 键位与命令

### 键位

| 键 | 功能 |
|---|---|
| `F7` | 重载 RTE 客户端资源（重新读取资源包里的轨道模型、脚本等，不用退出游戏） |
| `I` | 打开列车界面 |
| `O` | 打开 RTE 配置界面 |
| `R` | **对着轨道按下，直接打开轨道编辑界面**。不用手持刷子，准星对准轨道模型上的任意位置即可（不限于节点方块）。准星前方没有轨道时会提示「准星前方没有找到轨道」。 |

键位都可以在原版的「控制」设置里改。

### 命令

| 命令 | 说明 |
|---|---|
| `/rte repairrails` | 扫描并修复整个世界里损坏的轨道数据。需要 OP（权限等级 2）。 |
| `/rte fixoneway` | 修正单向轨的方向数据。需要 OP（权限等级 2）。 |

**调试没有命令入口。** 按 `O` 打开设置，开 `enableRenderDiagnostics`（渲染诊断）；
服务端诊断改存档目录下 `rte_config.json` 的 `enableNetworkDiagnostics`。

---

## 轨道编辑

这是 RTE 的主体。所有轨道编辑都从 **MTR 的轨道刷子** 进入。

### 入口

两种方式：

**方式一（推荐，不用换手持物品）** —— 准星对准轨道模型上的任意位置，按 `R`。

**方式二** —— 手持 MTR 轨道刷子对着轨道节点：

- **直接右键** —— 把刷子当前设置刷到这条轨道上（改属性，会立刻生效）
- **Shift + 右键** —— 打开 **轨道编辑界面**

注意别点错：不按 Shift 是直接修改轨道。

两者的区别：刷子只能点**轨道节点方块**，`R` 键是沿视线找最近的轨道曲线，所以对着两节点之间的轨道中段也能用。

### 轨道编辑界面 → 轨道线形工具

在轨道编辑界面里点「轨道线形工具…」，这里集中了几何相关的功能：

**轨道链** —— 见下节。

**平行线复制** —— 按给定间距（2～12 m）向左或向右复制出一条平行线路，用来快速铺双线。

**沿线阵列** —— 见「装饰与接触网」。

**接触网规划器** —— 见下文，装了 MSD 才显示。

### 轨道链

很多工具（平行线、沿线阵列、接触网规划器）作用于「链」，而不是单独一段轨道。

链默认是**自动**算出来的：从你选中的那段往两头延伸，**遇到道岔或断头就停**（连接数不等于 2 就收手）。

在「轨道链」一节可以改成手动控制：

1. 点「**改为手动控制**」接管当前链
2. 「**缩起点**」「**缩终点**」—— 从两头逐段砍，只保留想要的一部分。砍到会丢掉你选中那段时会被拦住并提示。
3. 「**起点端/终点端延伸到：**」—— 链端点外每个可去的方向列成一个按钮（标着节点坐标），点哪个就往哪边接一段。**这就是道岔分支选择** —— 岔口有几个分支就列几个。
4. 「**恢复自动**」—— 丢弃手动结果

手动链只对你接管时选中的那段轨道生效，换一段编辑会自动回到自动模式。链存在客户端内存里，**关游戏就没了**。

### 铁轨路径编辑器

用**铁轨路径编辑器**这个物品：

- 对着轨道**右键** —— 选中一段（按你面朝方向挑最接近的那条）
- **空手右键**（不指向方块） —— 打开路径编辑画布

画布叠加在世界上方，可以直接拖拽控制点改线形。右侧面板：

- **路径模式按钮** —— 在原版 / 自动贝塞尔 / 手动贝塞尔 / 曲线拟合之间循环切换。当前路径切不过去时按钮会灰掉，悬停有说明。
- **几何信息** —— 长度、模式、高度差、起终点坐标、`R1/R2/VR` 半径
- **质量评级** —— 带色圆点 + `R min ... | ...‰ | ... km/h`，绿/黄/红一眼看出线形好坏和限速
- **视图** —— `XZ`/`Y` 切换俯视/立面，`−` `+` 缩放，「适应」一键框住整段
- **操作说明** —— 可折叠，展开看画布的鼠标操作

画布操作：左键绘制/拖拽，右键拖动平移，滚轮缩放。

---

## 装饰与接触网

### 装饰物件

RTE 的装饰方块，用来摆放模型（支柱、雨棚等）。可以调整位移和旋转。它同时是「沿线阵列」的样板载体。

### 沿线阵列（照样板复制）

思路是：**先手工立好一根，再沿轨道复制**。

1. 在轨道旁用**装饰物件**建好一根完整支柱（必须是带 prefab 的装饰物件，普通方块不行）。想要正/反定位交替就立两根。
2. **站到样板旁边** —— 锚点以玩家为中心搜，范围只有水平 10 格、上下 6 格
3. 打开轨道线形工具 →「沿线阵列」，设间距（5～60 m）
4. 装了 MSD 的话还能选接触网线型，会顺带连线（MSD 节点在锚点周围 6 格内收集）
5. 点「沿线阵列最近的装饰」

间距 ≥20 m 时弯道会自动加密跨距。作用范围是**整条链**。

失败提示：

| 提示 | 原因 |
|---|---|
| 附近 10 格内没找到眼缘装饰样板 | 站太远，或样板不是带 prefab 的装饰物件 |
| 线路太短或无法沿线采样 | 整条链长度不足一个间距 |

### 接触网规划器（全程界面内完成）

**需要 MSD。** 不用立样板、不用管站位，全部在界面里做。

入口：轨道线形工具 →「接触网规划器…（全程界面内完成）」

**导线参数**

| 参数 | 范围 | 说明 |
|---|---|---|
| 节点方块 | 14 种 | 默认「长腕臂带配重」。前 6 项是带模型的接触网（就是创造栏里那些「接触网(带模型)」），后面是纯节点 |
| 接触网线型 | 4 种 | CATENARY / ELECTRIC / RIGID_CATENARY / RIGID_SOFT_CATENARY |
| 标准跨距 | 8～80 m | 相邻两根的目标间距 |
| 拉出值 | 0～1.5 m | 之字形幅度，逐根左右交替 = 正/反定位 |
| 导线高度 | 3～12 m | 轨面以上 |
| 整体横移 | ±8 m | 整条导线往一侧偏，之字形叠在这个中心上 |
| 起始里程偏移 | 0～60 m | 整排支柱沿轨道前后平移，用来错开跨距相位 |

**支柱方块偏移**（导线位置不变，拉出值自动补差）

| 参数 | 范围 |
|---|---|
| 支柱横移 | ±8 m |
| 支柱升降 | ±8 m |
| 支柱纵移 | ±8 m |

这组参数只挪**方块**，导线纹丝不动 —— MSD 的 `OffsetPosition` 本来就是「导线相对方块中心的偏移」，方块挪多少就自动反算进去。带模型的支柱如果锚点不在导线处，用这三个滑条把方块挪到该在的位置即可，不必去动导线高度将就。

**开关**

- 首根为正定位 —— 决定第一根往哪边拉，也就决定整条的相位
- 弯道自动加密 —— 半径 <800 m 收到 0.8 倍、<400 m 收到 0.65 倍
- 支柱在行进方向右侧 —— 决定支柱在哪一侧，朝向跟着翻
- 反定位自动用镜像件 —— 之字形拉负方向的那些根换成 `_mirror` 变体（只对带配重的两种有效，其它变体没有镜像版）

带模型的方块会**精确垂直于该点的轨道切线**，不是对齐到东南西北四个方向 —— MSD 的方块状态只能存 4 档朝向，剩下的角度用方块实体的连续旋转量补齐，所以弯道上每一根都正对轨道。纯节点方块（不带模型的那几种）没有旋转数据，仍是 4 向对齐。

支柱始终保持竖直，不跟随坡度俯仰。

**预览**（俯视图，拖动平移、右键上下拖动缩放）

- 灰线 = 轨道中心线
- 绿点 = 正定位，蓝点 = 反定位
- 青线 = 导线，能看出之字形
- **红线 = 这一跨 MSD 判定非法**，铺设时会跳过
- 灰色小方框 = 支柱方块落点（仅在偏离导线时画），细灰线连到对应导线点
- 放大后每跨中间标出米数

顶部汇总：链长、节点数、「全部跨距合法」或「非法跨距: N」。

确认预览没问题（尤其没有成片红线）后点「**铺设**」。

作用范围是**整条链** —— 想只铺一部分，先用前面的「轨道链」功能裁剪。

---

## 其他物品

| 物品 | 用法 |
|---|---|
| **路线路径创建器** | 手动串线路。右键轨道节点依次串联，tooltip 实时显示已连每段（起终点、长度、停站时间）。空手右键打开界面填线路名和颜色后建线。校验齐全：拒绝单向轨、起点必须是站台、起点不能折返、非折返轨/非站台之间不许折返，折返点自动补停站时间。 |
| **铁轨路径编辑器** | 见「轨道路径编辑器」一节 |
| **位移工具** | 右键轨道节点，按轨道几何折算把你传送到对应位置 |
| **桥梁创建器**（1/2/3 格宽） | 沿轨道铺桥面。台阶方块会按半砖高度自动选上/下半砖 |
| **复合创建器** | 组合体创建，带独立界面 |

## 其他方块

| 方块 | 说明 |
|---|---|
| **直线节点** | 强制该处为直线段 |
| **装饰物件** | 摆放装饰模型，也是沿线阵列的样板 |
| **发车铃** | 右键交互 |
| **留言箱** | 玩家留言 |
| **参观者统计闸机** | 统计通过人数 |

---

## 铁路仪表板增强

RTE 给 MTR 的铁路仪表板地图加了：

- **地形底图** —— 缩放足够时显示真实地形贴图
- **线路配色** —— 轨道按所属线路上色。用 A\* 在轨网上对每条线路逐区间寻路（代价 = 轨道实际长度，启发 = 到下一站的直线距离），把走过的轨道标成该线路颜色
- **刷新过程可视化** —— 重算时能看到已访问节点铺开、开集前沿高亮推进、轨道逐段染色
- **进度日志** —— 左上角显示 `A* 寻路 12/48 · 1号线 第 3/9 段 · 已扩展 4821 · 已着色 233`，下面滚动每段的结果（扩展了多少节点、着色多少段、失败原因）。完成后再留 8 秒显示总耗时。开着 `enableRenderDiagnostics` 时同样内容进日志文件。
- **线路名称标注**
- 缩放到一定程度线段变虚线

线路数量或轨道数量变化、或距上次超过 5 秒时自动重算。

---

## 资源包工作台

从 RTE 配置界面进入。用于查看和处理旧版资源包：预览车辆模型（按 bbmodel 的 outliner 分组还原部件位置）、导出资源包等。

---

## 服务端数据同步

MTR 客户端只同步玩家附近的数据。走远了仪表盘就缺轨道，车载显示也会因为路径被截断而算错时间。RTE 增加了三组按需请求：

| 内容 | 触发时机 | 说明 |
|---|---|---|
| 完整行车路径 | 脚本读取 `train.path()` 且本地不足 8 段时 | 修正资源包据此推算的均速与预计到站时间 |
| 全量轨道 | 打开仪表盘时，每个世界一次 | 分页传输，收完立即落盘 |
| 视口内列车位置 | 仪表盘打开期间，10 Hz | 带外推平滑；界面关掉即零流量 |

**这三项都需要服务端同样安装 RTE。** 纯 MTR 服务器上功能不生效，但不会更差 —— 自动退回原有行为。

仪表盘轨道另会采样成折线**持久化到本地**：

```
config/rte/mapcache/<世界标识>.rtemap
```

单人存档是 `local_存档名`，多人是 `server_地址`。带线路色的按原色画，归档中没有颜色的用暗灰 —— 灰色的就是历史数据。

---

## 配置

有两套配置，位置不同：

- **客户端配置** —— 客户端本地 `config/rte.json`，`O` 键或配置界面里改
- **世界配置** —— 存档目录下的 `rte_config.json`，手动编辑，服务端启动时读取

### 渲染与性能

| 选项 | 默认 | 说明 |
|---|---|---|
| BBMODEL 预加载优化 | 关 | 提升复杂 BBMODEL 车辆的渲染效率，代价是内存占用上升。**目前有缺陷：开启后接触网支柱等装饰模型不渲染，建议保持关闭**（见已知问题） |
| 轨道形变 | 开 | 轨道模型沿曲线形变 |
| 超高倾斜 | 开 | 车体、镜头、玩家站立平面跟随超高倾斜 |
| 显示 JS 调试信息 | 关 | 左上角叠加脚本调试信息 |
| `gpuTranslucentRendering` | 开 | 半透明模型使用 GPU VBO 与物体级远近排序，避免每帧在 CPU 重建全部透明面；复杂车辆通常可明显提升帧率 |
| `enableRenderDiagnostics` | 关 | 每 10 秒在日志汇总渲染阶段耗时、慢帧与模型去重统计；仅排查性能问题时开启 |
| `modelDedupeFaceLimit` | `50000` | 单个模型参与面去重的上限；超大型模型超过此值时跳过去重，避免加载卡顿 |
| `mtrMeshDedupeFaceLimit` | `2000` | 同上，但作用于 MTR 车辆网格。**不要调成 0** —— 实测「全量去重」反而因 HashMap 开销撑爆堆 |
| `translucentSort` | 开 | 半透明面排序 |
| `shaderCompatMode` | 关 | 光影兼容模式。开启后关闭 RTE 的渲染优化，用于排查与光影包的冲突 |
| `maxTextureSize` | `2048` | 超过此边长的贴图在读取时降采样到 2 的幂次以内。老包常带 8K 贴图，限到 2048/4096 可省下大量显存与堆 |
| `scriptTextureLimit` | `512` | 脚本动态贴图缓存上限 |
| `enableBbModelPreload` | 关 | BBMODEL 预加载（即上表首行的「BBMODEL 预加载优化」） |
| `bbModelPreloadLimit` | `24` | 预加载的车辆数量上限 |
| `preloadHeapCeilingPercent` | `35` | 预加载占用堆的百分比上限，超过即停止 |
| `enableRailRender` / `enableRail3D` / `enableRailDeform` | 开 | 轨道渲染、3D 轨道、沿曲线形变 |
| `enableTrainRender` / `enableTrainSound` | 开 | 车辆渲染与音效 |
| `enableRolling` | 开 | 超高倾斜 |
| `enableSmoke` | 开 | 烟雾粒子 |
| `hideRidingTrain` | 关 | 乘车时隐藏所乘车厢 |
| `railDistanceRendererInterval` | `5` | 轨道距离渲染的刷新间隔 |
| `railDistanceRendererMaxDistanceSqr` | `256` | 轨道距离渲染的最大距离平方 |
| `useEditBoxSetRailRolling` | 开 | 用输入框而非滑块设置超高 |

客户端配置中的渲染间隔、距离、预加载数量和模型面数都会在读取时限制到安全范围；配置文件写入采用临时文件替换，异常中断不会留下半截 JSON。

### 世界同步与诊断

| 选项 | 默认 | 说明 |
|---|---|---|
| `allowVehiclePathSync` | 开 | 允许客户端按需请求完整列车路径 |
| `allowVehicleMarkerSync` | 开 | 允许仪表盘请求视口内列车位置 |
| `allowFullRailSync` | 开 | 允许仪表盘分页同步全量轨道并建立本地归档 |
| `enableNetworkDiagnostics` | 关 | 在服务端日志汇总上述同步包的数量与字节数；日常使用保持关闭 |

列车路径与位置请求在服务端按玩家限频；全量轨道按轨道 ID 去重并稳定排序，避免双向索引造成重复传输和分页抖动。

### 车辆兼容

| 选项 | 默认 | 说明 |
|---|---|---|
| `scriptEngine` | `rhino` | 脚本引擎，可填 `rhino` / `graal` / `auto` |
| `builtinGangway` | 开 | 绘制 RTE 自己的车厢连接管 |
| `forceGangwayOnAllCars` | 开 | 编组内部相邻车厢强制连通。按车厢位置与司机室端判定，不会在车头车尾外侧或双头编组的司机室对接处生成 |
| `closeCouplingGap` | 关 | 开启后强制车钩间隙为 0，车厢贴得更紧。关闭时尊重资源包定义 |
| `legacyVariationCarFilter` | 开 | 修正 MTR 4 把旧版车辆拆成单节变体导致的车头车厢重叠 |
| `vanillaVehicleMode` | 关 | **排查开关** —— 打开后一次性关闭上述全部车辆改动，用来判断问题是否由 RTE 引起 |
| `legacyWalkways` | 开 | 3.x 包的门洞/地板合成。按门的位置推导 `DOORWAY` 部件并注入转换结果，实测可对一节车合成 4 个门洞 |
| `acceptLegacyPackFormats` | 开 | 放宽资源包 `pack_format` 校验，接受为更早 MC 版本打包的老包 |
| `legacyDefaultTextureFallback` | 开 | MTR 4 只认路径严格等于 `default.png` 的基础贴图，老包常写成 `xxx/default.png`。开启后改为按文件名匹配 |
| `legacyVehicleTakeover` | 关 | **实验性** —— 由 RTE 直接解析老包的 obj 车辆定义并接管渲染，绕开 MTR 的转换链。仅对 obj 车生效，bbmodel 车不受影响 |
| `autoGangwayPolicy` | 开 | 资源包自带贯通道（`gangway_connection_id` 为空或指向 `air`）时，自动抑制内置贯通道，避免重叠 |
| `fixLegacyPlugDoors` | 关 | 老包塞拉门的开门动画修正 |
| `mirrorReversedDoorways` | 关 | 反向车厢的门洞镜像 |
| `traceDoorSides` | 关 | 排查用 —— 在日志打印每节车的开门侧判定 |
| `legacyFlippedPositionFix` | `off` | 排查用 —— 为缺失 `positionsFlipped` 的位置定义补齐数据，可填 `off` / `copy` / `mirror`。**经查证 MTR 4 的镜像转换本身是正确的，此项无需开启** |
| `jcmJsChoice` | `0` | JCM 脚本兼容策略 |

排查车辆显示问题时，第一步就是把 `vanillaVehicleMode` 打开对照一次 —— 现象消失说明是 RTE 的改动引起，逐项打开即可定位。

---

## 已知问题

以下是**已知未解决**的：

- **开启「BBMODEL 预加载优化」后，接触网支柱等装饰模型不渲染。** 已用对照实验确认（同一相机位置，开启时支柱消失、关闭时正常），原因是 MTR 的 `CachedResource` 用一个**静态闸门**限制「每 tick 只加载一个资源」，而预加载 551 个车辆时会反复关闭这个闸门，导致其它模型的加载被卡死。**规避办法：关掉这个选项。**
- 资源包导出的素材链不完整（obj 的 .mtl、bbmodel 内嵌贴图、音效、多车辆合并）
- **部分老包车辆的车头/车尾镜像异常。** 已排除两条路径：RTE 不改动老包车辆坐标（停用全部 15 个车辆 mixin 后现象仍在），MTR 4 的 `mirror`/`reversed` 转换经反编译逐case验证也是正确的。**根因仍未定位。**
- **部分老包车辆的车厢位置错位**（万方 4 号线、重庆 crt333）。与上一条无关，同样未定位。
- `legacyVehicleTakeover` 为实验性功能，几何数据已验证正确（车厢间距、部件过滤、门数量、条件分组均符合预期），但**视觉表现尚未逐项确认**。

## 尚未充分测试

以下功能代码完整但**尚未在游戏内逐项验证**，使用时请留意：

- 接触网规划器：支柱朝向可能需要用「朝向微调」旁的 0°/90°/180°/−90° 四个按钮点一次校准，哪个正对轨道就用哪个
- 轨道链手动选择
- 铁路仪表板的线路配色与寻路动画

遇到问题时按 `O` 打开设置，开 `enableRenderDiagnostics`，日志在 `logs/latest.log`。
