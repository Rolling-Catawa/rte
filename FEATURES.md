# RTE 功能列表

Rollingcats Transit Expansion — 面向 MTR 4.0 的轨道几何与建轨工具
Minecraft 1.20.1 · Fabric / Forge · 依赖 MTR 4.0.x

---

## 轨道几何

- **四种路径模式**：原生 / 自动贝塞尔 / 多点曲线拟合 / 手动贝塞尔
- **竖曲线**：可设半径，坡度以 ‰ 显示
- **超高（侧倾）**：沿轨道分布的滚转角，车辆与镜头同步倾斜
- **等弧长采样**：统一 `TrackPath` 接口，提供位置 / 切线 / 法线 / 侧向
- **几何分析**：曲率、最小半径、坡度评级

## 建轨工具

| 物品 | 用途 |
|---|---|
| 轨道路径编辑器 | 按当前路径模式打开对应编辑器 |
| 线路路径创建器 | 沿线路批量建轨 |
| 复合创建器 | 组合任务批处理 |
| 位移工具 | 整体平移轨道 |

- **形状工具**：双线复制、沿线阵列、接触网规划
- **轨道链**：手动接管链条，两端收缩 / 延伸；支持世界内拖动端点手柄（吸附到相邻节点）
- **撤销**：蹲 + 右键空气

## 装饰方块

直连节点 · 装饰物件

装饰物件支持自定义模型、脚本驱动与预制体注册表。

## MSD 整合

接触网规划器：沿轨道自动布置支柱与导线，可调跨距、拉出值、高度、侧别、曲线自适应加密。
软依赖 —— 未安装 MSD 时该功能自动隐藏。

## 脚本

- **双引擎运行时**：GraalJS / Rhino，资源包声明 `"engine"` 即可切换
- 列车脚本、装饰物件脚本、LCD 动态纹理
- Rhino 时代的资源包可原生运行

## 渲染

- 自定义轨道模型（批渲染管线，支持实例化）
- 视角滚转
- 轨道预览、测距、节点角度可视化

## 界面

- ImGui 全套界面：多窗口并存、拖动、停靠、折叠、布局跨会话记忆
- 资源工作台 + 资源包导出
- 仪表板地图增强：轨道叠加、线路配色、A\* 寻路可视化

## 配置项

38 个客户端开关，写在 `config/rte.json`，`O` 键打开界面。分四组：

- **渲染与性能**：轨道渲染等级 · 轨道形变 · 视角滚转 · 半透明排序 · 贴图尺寸上限 · 模型面去重阈值 · 光影兼容模式
- **预加载**：BBModel 预加载及其数量与堆占比上限
- **世界同步与诊断**：路径 / 位置 / 全量轨道同步开关 · 渲染与网络诊断
- **车辆兼容**：见下节

完整表格见 `MANUAL.md`。

## 兼容性适配

面向 MTR 3.x 时代资源包的兼容层，逐项可关：

- **贴图**：`default.png` 按文件名匹配（MTR 4 要求路径严格相等，老包多写作 `xxx/default.png`）
- **包格式**：放宽 `pack_format` 校验，接受为更早 MC 版本打包的老包
- **非法路径**：扫描资源包内不合法的条目路径并生成合法别名
- **门洞与地板**：按门位置推导 `DOORWAY` 部件注入转换结果（3.x 格式无此概念，MTR 4 却需要）
- **贯通道**：资源包自带贯通道时自动抑制内置贯通道，避免重叠
- **车辆变体**：修正 MTR 4 把旧版车辆拆成单节变体导致的车头重叠
- **脚本**：Rhino 时代的资源包脚本原生运行，按函数失败退避避免异常风暴
- **超大贴图**：读取时降采样，老包常带的 8K 贴图不再撑爆显存

实验性：`legacyVehicleTakeover` 由 RTE 直接解析老包 obj 车辆定义并接管渲染，绕开 MTR 转换链。

经测试适配MTR3的大多数资源包（无需改包，包括LCD）